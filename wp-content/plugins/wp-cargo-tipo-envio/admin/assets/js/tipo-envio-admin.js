/* WP Cargo Tipo de Envio — Admin JS v2.5
 * La lógica principal está en las páginas PHP (tarifario.php, cotizador.php)
 * Este archivo es el punto de enqueue para estilos y scripts globales admin.
 */
(function($){
    if(typeof WPCTE_ADMIN === 'undefined') return;
    // Reservado para extensiones futuras
})(jQuery);
